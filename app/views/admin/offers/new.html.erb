<%= render "layouts/adnav" %>
<div class="main mt-4">
  <div class="container">
    <h1><b>Create Offer</b></h1>
    <%= render "shared/error", resource: @offer %>
    <div class="card w-100 mt-3 shadow border-light">
      <div class="card-body bg-light">
        <%= form_for @offer, url: admin_offers_path, method: :post do |f| %>
          <%= render "form", f: f %>
          <div class="text-center mt-3 mb-3">
            <%= link_to 'Back', admin_offers_path, class: "btn btn-warning px-5 mx-3 shadow-sm" %>
            <%= f.submit "Create", data: { confirm: 'Are you sure you want to create?' }, class: "btn btn-success px-5 mx-3 shadow-sm" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>